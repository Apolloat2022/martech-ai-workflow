# Fictional reference scenario authored as an architecture portfolio piece. Not a record of a client engagement.
#
# Copy to .env to opt into the real model-provider adapter. With no .env file
# (or with this var unset/empty), the gateway defaults to the deterministic
# mock provider and runs with zero external API keys - see PROJECT_PLAN.md
# open question 2.

ANTHROPIC_API_KEY=
